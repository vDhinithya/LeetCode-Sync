# Write your MySQL query statement below
SELECT p.project_id ,
       round(
         avg(experience_years),2) as average_years
FROM project p
join employee e
on p.employee_id = e.employee_id
GROUP BY p.project_id;